pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'project started'
            }
        }

        stage('Test') {
            steps {
                echo 'running tests'
            }
        }

        stage('Deploy') {
            steps {
                echo 'deploying application'
            }
        }
    }
}
